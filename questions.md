## I wonder if the share buttons would look better if the text/icon color was --clr-light instead of --clr-accent. maybe --clr-accent on hover (like we do with teh button border). thoughts?

## "Legit error but a non-issue. Next.js renders <Script strategy="afterInteractive"> in the body, not <head> — GSC's GA verification specifically requires the snippet in <head>. Since you already have the meta tag method set up, you don't need GA verification too — skip it entirely." <-- what did you mean by this? its a legit error, but its fine?

## i wonder if --clr-dark should change to hsl(var(--hue) 20% 5.88%). right now it has a different hue/saturation than clr-mid, so things look a touch off to me.
