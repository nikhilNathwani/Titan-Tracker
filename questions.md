## "You can't cleanly derive one from the other because the shapes also differ structurally (e.g. BestScoreRow has titan_name for grouping; BestScore doesn't)." <-- does this make sense? how come BestScore doesn't habe titan_name? also, in BestScore, I see that titan_score and max_score are types as string, shouldn't they be some kind of number type, isnt that the point of "the DTO (Data Transfer Object) pattern — Row = wire format from pg (all numerics come out as strings), processed type = domain model"?

## In the google search console site there was a button to verify Google Analytics, so I clicked Verify, and it resulted in "Ownership Verification Failed", with the reason "Verification method:

Google Analytics
Failure reason:
The Google Analytics tracking code on your site is in the wrong location on the page. You can verify site ownership with the tracking snippet placed in the <head> section of your home page. Learn more
Please fix your implementation and reverify, or use another verification method." is this legit? i do see data when i go in the google analytics dashboard..

## I also got "Ownership verification failed" when verifying the meta tag for the google-site-verification thing, here was the details: "Verification method:

HTML tag
Failure reason:
We couldn't find your verification meta tag.
Please fix your implementation and reverify, or use another verification method.
" is it just a matter of things not propagating yet? or something else?

## should the card border of the hero section and notes card change to be --clr-accent instead of the current faint color? and/or should the stat card borders change to match the current faint border of the hero section & notes card? im still just feeling like the card border coloring feels a little unintentional/not cohesive somehow.

## now that widgets have clr-light isntead of clr-accent, should we do the same for the percentage value in the "team record" card? i.e. where we currently show "55.6%", could/should that be --clr-light instead of --clr-accent?
